# 2026-09-17 项目检查与修复记录

本记录汇总 2026-09-17 基础代码审查阶段发现的问题与修复依据。后续推荐服务接入见 [推荐服务说明](semantic-ranking.md)，0917 版本整体变更见 [README](../README.md)。

检查涉及认证与注册、管理权限、画像、匹配与双选、资源与配额、AI 对话与订阅、Excel 导入导出、前端请求与状态管理，以及 Docker、Nginx、CI 和数据库迁移。采用源代码审阅、静态检查、回归测试、独立 PostgreSQL 验证和依赖审计；这不等同于逐条业务流程的浏览器验收或商用安全认证。

## 已修复的问题

| 范围 | 问题及修复 |
| --- | --- |
| 管理端用户列表 | 基础响应已包含 `phone`，管理端再次传入导致运行时异常；移除重复参数，覆盖学生和导师两种响应。 |
| 认证令牌 | 普通接口强制校验 `authenticated` 用途；JWT 必须包含有效期、签发时间、主体、认证版本与用途，注册令牌不能用于普通接口。 |
| 注册会话 | 重启激活和完成注册递增认证版本；等待激活记录锁后重新核验版本，作废旧会话。 |
| 登录与激活 | 未知账户／Access Key 也执行虚拟密码校验，减少明显的时间差；虚拟哈希不可能成为有效凭据。 |
| 初始管理员 | 优先以固定用户名定位管理员，拒绝把碰巧占用初始化邮箱的普通账户提升为管理员；PostgreSQL 启动初始化增加事务锁。 |
| 双向选择 | 已取消或拒绝的申请重新提交时重新检查志愿数量；服务端拒绝同一租户内跨院校申请和邀请。 |
| 双选配置并发 | 租户、导师和学生设置首次创建使用冲突安全插入；加锁读取刷新会话中的旧数据。 |
| 管理权限 | 管理用户的公共判断补充租户一致性；前端超级管理员页面增加对应路由限制。 |
| AI 上下文 | 修复零剩余预算下 `[-0:]` 取出完整内容的问题；优先保留最近消息，再恢复时间顺序。 |
| AI 退款 | 加锁时刷新账务对象，防止旧会话重复退款；旧订阅周期的退款不增加新周期余额。 |
| 订阅与配额并发 | 首次订阅初始化增加账户锁；周期刷新始终锁定最新订阅状态，配额读取刷新旧值，防止并发覆盖扣费。 |
| AI 流式连接 | 未完成或主动关闭的生成器回收预占额度和未完成消息；上游必须收到 `message_stop`，不能把截断结果标记为完成。 |
| AI 持久化 | 答复与用量完成状态在同一事务提交；上游客户端和流均显式释放，设置请求超时并关闭自动重试。 |
| 浏览器 SSE | 支持跨字节 UTF-8、跨块及多行事件；收到完成事件立即返回，异常和完成后释放读取器。 |
| 前端错误处理 | 支持 FastAPI 的结构化校验错误，显示字段和原因，避免 `[object Object]`；保留 HTTP 状态码，不展示校验错误中的原始输入。 |
| 登录状态恢复 | 网络故障和服务端临时错误不再清除会话；401 才清除；退出登录后的迟到响应不能恢复旧用户。 |
| 存储配额 | 配额字节字段改为 BIGINT，修复允许设置 2048 MB 却发生 PostgreSQL 整数溢出的问题。 |
| 资源并发与文件一致性 | 首次配额创建冲突安全，删除资源时锁定记录和配额；数据库提交失败保留文件，提交后序列化失败也保留已保存文件。 |
| 上传与下载 | 拒绝文件后关闭上传句柄；取消上传清理暂存文件；下载固定为附件内容类型，并设置 `no-store`、`nosniff`。 |
| 资源校验 | URL 使用结构化校验并拒绝内嵌凭据；拒绝空白标题；课程时长不再静默截断超长数字。 |
| Excel 导入 | 预注册每次最多 1000 行，限制表头列数，忽略不可信工作表尺寸并关闭工作簿；订阅回执扫描限制为 32 列、2000 行。 |
| 导入响应 | Excel 解析和哈希处理改在同步端点工作线程执行；预注册密钥回执设置禁止缓存。 |
| 工作台性能 | 统计在数据库聚合，只加载最近 8 条动态，移除无用的画像预加载查询。 |
| 运维配置 | 健康检查确认数据库可查询；API/Web 增加重启策略；修复大写 UUID 绕过 AI 专用代理规则的问题，改密接口增加限流。 |
| 构建与迁移 | Docker 构建排除私有环境文件和密钥；Alembic 正确转义 URL 中的百分号，并声明路径分隔规则。 |
| CI | 加入独立 PostgreSQL 服务，自动执行新增迁移／并发测试；工作流默认仅有仓库读取权限。 |
| 依赖 | Vitest 从 3.x 升至 4.1.11，清除开发依赖的已知文件读取漏洞；前端依赖锁文件同步更新。 |

## 验证结果

- 后端：**79 项通过**，其中 **5 项**使用独立 PostgreSQL 16，覆盖完整迁移链、迁移回退再升级、2 GB 配额、订阅／资源／双选首次并发创建和 AI 并发额度限制。
- 前端：**14 项通过**，Vue／TypeScript 检查和 Vite 生产构建通过。
- Python 静态检查：Ruff 的 `E9,F63,F7,F82,F811,F401,F841` 规则通过，覆盖语法、未定义名称和无用导入／变量等问题；未做大范围代码格式改写。
- 前端 `npm audit`：包含开发依赖，**0 项已知漏洞**。
- 后端 `pip-audit -r requirements.txt --no-deps --disable-pip`：直接依赖 **0 项已知漏洞**。此项不代表完整传递依赖或容器镜像扫描。
- Nginx 配置使用实际镜像通过 `nginx -t`；Compose 使用 `.env.example` 和 `--no-env-resolution` 校验，避免读取真实配置。
- Git 差异空白检查通过。

回归测试使用独立测试配置，不加载项目 `.env`。PostgreSQL 测试为每次运行创建随机 schema，并在结束后清理；本次临时数据库与项目数据库隔离。

在 `backend` 目录运行 `python -m pytest -q`。若设置 `ACADEMICNEXUS_TEST_POSTGRES_URL`，会额外启用 PostgreSQL 验证；该地址应指向独立测试库，账户需要创建 schema 的权限。未配置时明确跳过 5 项 PostgreSQL 检查。

## 应用变更时注意

- 新增迁移 `20260917_0014_resource_quota_bigint.py`，发布代码时需要执行 `alembic upgrade head`。本次只在临时库验证了迁移。
- 若已有配额超过 32 位整数范围，回退该迁移会拒绝执行，避免静默截断；需要先处理超范围值。
- 注册令牌现在严格隔离；缺失必要声明的旧令牌需要重新登录。双选账户必须具备有效且一致的院校简称。
- 每批预注册最多 1000 行，前端中英文提示已同步。
- 资源记录删除成功但磁盘清理失败时，保留错误日志和磁盘残留文件，不撤销已经提交的删除；运维需要关注该日志并清理孤立文件。

## 商用验收仍需完成

本次没有读取真实配置、用户数据或线上日志，因此没有确认 TLS、生产 CORS、存储目录权限、备份恢复、监控告警和密钥轮换的实际配置。没有调用真实 MiniMax，也没有进行付费请求、真实用户数据验收、浏览器全流程测试或容量压测。

规模化前应继续完善管理列表与资源接口的服务端分页、管理员操作审计、全量依赖锁定及镜像扫描，并为 AI 请求增加业务幂等机制和进程异常退出后的账务核对。现有 SQLite 单元测试与小规模 PostgreSQL 并发测试不能替代生产负载和故障恢复演练。

技术核对参考：[PyJWT 声明校验](https://pyjwt.readthedocs.io/en/stable/usage.html)、[SQLAlchemy 刷新已加载对象](https://docs.sqlalchemy.org/en/20/orm/queryguide/api.html#populate-existing)、[Vitest 修复公告](https://github.com/advisories/GHSA-82fw-gwwq-j7x9)。
